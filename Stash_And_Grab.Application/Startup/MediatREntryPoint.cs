namespace Stash_And_Grab.Application.Startup;

public static class MediatREntryPoint
{
    // Do not add any code here
    // This is a pointer class so the AddMediatR can scan this assembly to register services.
}